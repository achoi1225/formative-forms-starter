extends layout.pug

block content

    h2 Create User
    if errors
        div
            ul
                each error in errors
                    li #{error}
    form(method="post" action="/create")
        input(type="hidden" name="_csrf" value=csrfToken)
        div(class="form-group")
            label(for="firstName")
            input(type="text" class="form-control" placeholder="First name" name="firstName" value=firstName)

        div(class="form-group")
            label(for="lastName")
            input(type="text" class="form-control" placeholder="Last name" name="lastName" value=lastName)

        div(class="form-group")
            label(for="email")
            input(type="email" class="form-control" id="inputEmail3" placeholder="email" name="email" value=email)

        div(class="form-group")
            label(for="password")
            input(type="password" id="inputPassword5" class="form-control" aria-describedby="passwordHelpBlock" placeholder="Password" name="password" value=password)
            small(id="passwordHelpBlock" class="form-text text-muted") Your password must be 8-20 characters long, contain letters and numbers, and must not contain spaces, special characters, or emoji.

        div(class="form-group")
            label(for="confirmedPassword")
            input(type="password" id="inputPassword5" class="form-control" aria-describedby="passwordHelpBlock" placeholder="Confirm Password" name="confirmedPassword")

        input(type="submit" value="Create User" placeholder="submit")
//- class="btn btn-primary form__button"
